mutation AddTeamMember($teamId: String!, $username: String!) {
  addTeamMember(teamId: $teamId, username: $username) {
    errors {
      ...ErrorsFragment
    }
    data {
      ...TeamFragment
    }
  }
}
