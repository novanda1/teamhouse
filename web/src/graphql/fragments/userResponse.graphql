fragment UserResponseFragment on UserResponse {
  errors {
    ...ErrorsFragment
  }
  tokens {
    accessToken {
      token
      expires
    }
    refreshToken {
      token
      expires
    }
  }
  user {
    _id
    username
  }
}
